type CreateTestExecutionResponse {
    testExecutionId: ID! # TODO: Switch to returning the TestExecution directly
    testExecutionGroupId: ID!
}

extend type Mutation {
    createTestExecution(
        runID: String!
        testName: String!
        featureFile: String!
    ): CreateTestExecutionResponse!
}
