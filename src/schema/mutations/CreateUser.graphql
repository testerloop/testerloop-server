type User {
    id: ID!
    email: String!
    firstName: String!
    lastName: String!
}

input UserInput {
    email: String!
    firstName: String!
    lastName: String!
}

extend type Mutation {
    createUser(userinput: UserInput!): User!
}
