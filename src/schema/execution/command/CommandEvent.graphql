interface CommandEvent implements Event & TestExecutionEvent {
    at: DateTime!
    testExecution: TestExecution!
}

type CommandEventConnection {
    totalCount: Int!
    edges: [CommandEventEdge!]!
    pageInfo: PageInfo!
}

type CommandEventEdge {
    cursor: Cursor!
    node: CommandEvent!
}
