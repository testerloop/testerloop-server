type TestExecutionScreenshot  implements Event & InstantaneousEvent & TestExecutionEvent {
    at: DateTime!
    testExecution: TestExecution!
    """
        Returns a signed URL for fetching this screenshot from S3.
    """
    url: SignedURL!
}
