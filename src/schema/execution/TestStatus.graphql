enum RunStatus {
    RUNNING
    COMPLETED
}

enum TestStatus {
    FAILED
    PASSED
    IN_PROGRESS
}

type TestExecutionStatus {
    id: ID!
    testName: String!
    featureFile: String!
    testStatus: TestStatus!
    rerunOfId: TestExecution
}

type TestRunStatus {
    runStatus: RunStatus!
    testExecutionStatuses: [TestExecutionStatus]!
}

extend type Query {
    getRun(runId: ID!): TestRunStatus!
}

extend type Mutation {
    setTestExecutionStatus(
        testExecutionId: ID!
        testStatus: TestStatus!
    ): TestExecutionStatus!
}
