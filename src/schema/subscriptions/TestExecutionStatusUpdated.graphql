extend type Subscription {
    testExecutionUpdated(id: ID!): TestExecutionStatusUpdatedEvent!
}

type TestExecutionStatusUpdatedEvent implements Event & InstantaneousEvent {
    at: DateTime!
    testExecution: TestExecution!
}
